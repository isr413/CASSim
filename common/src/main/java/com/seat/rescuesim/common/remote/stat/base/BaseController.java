package com.seat.rescuesim.common.remote.stat.base;

import com.seat.rescuesim.common.remote.stat.StaticRemoteController;

public class BaseController extends StaticRemoteController {

    public BaseController(String remoteID) {
        super(remoteID);
    }

}
